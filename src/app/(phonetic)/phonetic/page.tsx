import { Page } from "@/components/page-container";
import List from "./components/list";

// 主应用组件
const App: React.FC = () => {
  return (
    <Page>
      <List />
    </Page>
  );
};

export default App;
